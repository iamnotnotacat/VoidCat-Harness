# VoidCat Harness does not read Hunter-Seeker credentials from environment files.
# Add and remove provider secrets through the in-app setup guide so Electron can
# encrypt them with Windows protected storage. Secret values are never exposed to
# the renderer, logs, source control, chat context, or UNIT context.

# Reserved prefix for future non-secret deployment configuration only:
# VC_HUNTER_SEEKER_
